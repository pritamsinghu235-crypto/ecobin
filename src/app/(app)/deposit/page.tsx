import { ComingSoon } from "@/components/layout/coming-soon";

export default function DepositPage() {
  return (
    <ComingSoon
      title="Deposit"
      phase="Phase 4 · IoT Simulator"
      description="A guided deposit flow with live AI plastic classification, streamed from the device simulator and (later) real ESP32 machines."
    />
  );
}
